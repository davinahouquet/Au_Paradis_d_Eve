{% extends 'base.html.twig' %}
{% block meta_description %} Réservations à venir {% endblock %}
{% block css %}
    <link rel="stylesheet" href="{{ asset('css/base.css') }}">
    <link rel="stylesheet" href="{{ asset('css/profil.css') }}">
{% endblock %}

{% block body %}


<div class="profil">

    <h1>Réservation à venir</h1>
    
    {% if app.user %}
        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('toutes_reservations') }}"  type="button" class="btn btn-light" id="retour">Retour</a>
        {% else %}
            <a href="{{ path('app_user') }}"  type="button" class="btn btn-light" id="retour">Retour au profil</a>
        {% endif %}
    

    {% if is_granted('ROLE_ADMIN') %}
        {# {% for reservationAVenir in toutesReservationsAVenir %} #}
            <table class="table table-warning table-striped">
                <thead>
                <tr>
                    <th scope="col">N°</th>
                    <th scope="col">Nom Prénom</th>
                    <th scope="col">Dates</th>
                    <th scope="col">Chambre</th>
                    <th scope="col">Durée</th>
                    <th scope="col">Option(s)</th>
                    <th scope="col">Prix total</th>
                    <th scope="col">Action</th>
                </tr>
                </thead>
                <tbody>
                    {% for reservationAVenir in toutesReservationsAVenir %}
                <tr>
                    <th scope="row">{{ reservationAVenir.id }}</th>
                    <td>{{ reservationAVenir.prenom }} {{ reservationAVenir.nom }}</td>
                    <td>{{ reservationAVenir.dateDebutFr }} au {{ reservationAVenir.dateFinFr }}</td>
                    <td>{{ reservationAVenir.espace.nomEspace }}</td>
                    <td>{{ reservationAVenir.duree }} nuits</td>
                    <td>
                        {% if reservationAVenir.options %}                      
                            {% for option in reservationAVenir.options %}
                                * {{ option.nom }} <br>
                            {% endfor %}
                        {% else %}
                            <p>Pas d'option</p>
                        {% endif %}
                    </td>
                    <td>{{ reservationAVenir.prixTotal }} €</td>
                    <td><a href="{{ path('annuler_reservation', {'id' : reservationAVenir.id})}}">Annuler</a>
                    </td>
                </tr>
                {% endfor %}  
                </tbody>
            </table>
        {# {% endfor %}   #}
    {% endif %}

    {% if reservationsAVenir %}
        {# {% for reservationAVenir in reservationsAVenir %} #}
                <table class="table table-warning table-striped">
                    <thead>
                    <tr>
                        <th scope="col">N° réservation</th>
                        <th scope="col">Nom Prénom</th>
                        <th scope="col">Dates</th>
                        <th scope="col">Chambre</th>
                        <th scope="col">Durée</th>
                        <th scope="col">Option(s)</th>
                        <th scope="col">Prix total</th>
                        <th scope="col">Action</th>
                    </tr>
                    </thead>
                    <tbody>
                        {% for reservationAVenir in reservationsAVenir %}
                    <tr>
                        <th scope="row">{{ reservationAVenir.prenom }}</th>
                        <td>{{ reservationAVenir.prenom }} {{ reservationAVenir.nom }}</td>
                        <td>{{ reservationAVenir.dateDebutFr }} au {{ reservationAVenir.dateFinFr }}</td>
                        <td>{{ reservationAVenir.espace.nomEspace }}</td>
                        <td>{{ reservationAVenir.duree }} nuits</td>
                        <td>
                            {% if reservationAVenir.options %}                      
                                {% for option in reservationAVenir.options %}
                                    * {{ option.nom }} <br>
                                {% endfor %}
                            {% else %}
                                <p>Pas d'option</p>
                            {% endif %}
                        </td>
                        <td>{{ reservationAVenir.prixTotal }} €</td>
                        <td><a href="{{ path('annuler_reservation', {'id' : reservationAVenir.id})}}">Annuler</a>
                        </td>
                    </tr>   
                    {% endfor %} 
                    </tbody>
                </table>
            {# {% endfor %}   #}
        {% endif %}
    {% else %}
        <p>Pas de réservation à venir</p>
    {% endif %}

    <div class="rappel">
        <p>Modalités d'annulations :</p>
        <ol>
            <li><b><span style="color:#FFA800">REMBOURSABLES</span></b> : Les réservations dont l'annulation est survenue 30j avant la date de début du séjour, ou à n'importe quel moment par le responsable de l'établissement font l'objet d'un remboursement total.</li>
            <li><b><span style="color:#FFA800">REMBOURSEMENT PARTIEL</span></b>: Les réservations dont l'annulation est survenue 20j avant la date de début du séjour font l'objet d'un remboursement de 30% du prix total. <br></li>
            <li><b><span style="color:#FFA800">NON REMBOURSABLE</span></b>: Les réservations survenues moins de 20j avant la date de début du séjour ne feront l'objet d'aucun remboursement.</li>
        </ol>
    </div>

    </div>
{% endblock %}